import Header from '../components/layout/Header'

// Placeholder for the Header's third icon (lightbulb, unlabeled in the
// Claude Design canvas). Fill in once that feature is actually scoped.
export default function TipsPage() {
  return (
    <div>
      <Header />
      <main className="mx-auto max-w-2xl px-4 py-16 text-center text-ink/50">
        Tips — coming soon.
      </main>
    </div>
  )
}
